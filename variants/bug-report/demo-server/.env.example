# Demo server configuration
# Copy this file to .env and adjust as needed.

# Hostname / IP address the server binds to (default: localhost)
# Set to 0.0.0.0 to listen on all network interfaces.
HOST=localhost

# Port the HTTP server listens on (used when no TLS certs are found, default: 3000)
PORT=3000

# Port the HTTPS server listens on (used when TLS certs are present, default: 3443)
HTTPS_PORT=3443

# Winston log level: error | warn | info | verbose | debug | silly (default: info)
# Set to 'verbose' to see full bug-report payloads in the console.
LOG_LEVEL=info

# Optional: custom paths to TLS certificate and key files.
# By default the server looks in ./certs/cert.pem and ./certs/key.pem.
# TLS_CERT=./certs/cert.pem
# TLS_KEY=./certs/key.pem
